# Memoria diaria — 2026-03-12

## Sesión actual
- Pedido de Guille: repasar lo último realizado en `modulos/M6/web` y guardarlo en memoria.
- Se leyó el contexto base del workspace (`../SOUL.md`, `../USER.md`, `../MEMORY.md`) y la memoria local del proyecto (`modulos/M6/web/MEMORY.md`).
- No existían notas diarias en `memory/`; se crea esta carpeta hoy para persistencia de contexto futuro.

## Proyecto revisado
- Proyecto: web de clases de la Diplomatura IA M6.
- Ruta: `/home/guille/.openclaw/workspace/Diplomatura-Inteligencia-Artificial/modulos/M6/web`

## Último trabajo confirmado
- Los archivos más recientes del proyecto son:
  - `modulos/M6/web/styles.css` — 2026-03-12 11:41
  - `modulos/M6/web/script.js` — 2026-03-12 11:41
  - `modulos/M6/web/index.html` — 2026-03-12 11:32
- Estado funcional reconstruido a partir del código:
  - visor de slides con indicador `1 / N`;
  - navegación con teclado (`ArrowLeft`, `ArrowRight`, `Escape`);
  - badge de carga/error visible;
  - panel de detalle flotante con contenido real desde `../notas-atomicas/`;
  - render básico de Markdown en cliente;
  - mitigación de renders asíncronos fuera de orden;
  - ajustes responsive y mejoras de accesibilidad.
- Se verificó que la memoria local del proyecto ya tenía la mayor parte del contexto y se actualizó con este repaso validado.

## Observaciones
- `git log` no devuelve commits todavía en este repo (`master` sin commits).
- Conviene mantener actualizado `modulos/M6/web/MEMORY.md` hasta que se haga la revisión final y eventual deploy.

## Cambio posterior
- Se deshabilitaron temporalmente los slides 1 y 2 en `modulos/M6/web/script.js`.
- La navegación quedó limitada al slide 0 y los controles de avance/retroceso se desactivan cuando solo hay un slide habilitado.
- Más tarde se volvió a habilitar el slide 1 para incorporar un nuevo MOC de M6 basado en el contexto adjunto por Guille.
- `modulos/M6/web/data.json` quedó reemplazado con la nueva red conceptual:
  - nodo hub `MOC - ChatGPT en la Preparación de Entrevistas de Trabajo`;
  - nodos de CV, bios, entrevistas, correos y prompting alineados al contexto adjunto;
  - varios nodos enlazados a notas atómicas locales reales (`importancia-curriculum.md`, `pasos-claves-curriculum.md`, `desarrollo-biografias-impactantes.md`, `analisis-oferta-laboral.md`, etc.).
- Ajuste posterior de presentación: el panel del slide ya no expone secciones descriptivas/internas de las notas atómicas.
- Se filtran al vuelo `Enlaces`, `Prompt sugerido`, `Aplicación web interactiva` y `Pendientes` para mostrar solo contenido docente más limpio.
- Ajustes posteriores de UI:
  - se eliminó el texto superior de ayuda `Arrastrá nodos...`;
  - el título del slide M6 quedó en `M6: empleabilidad con IA`;
  - el subtítulo del slide M6 se vació.
- Ajuste posterior de interacción:
  - se restauró la flotabilidad del panel de notas;
  - el panel vuelve a seguir el mouse cuando está abierto.
- Ajuste posterior de layout del slide M6:
  - se estabiliza la simulación antes de dibujar;
  - se centra el grafo respecto del área útil visible;
  - se abre la distribución en horizontal para evitar una composición demasiado vertical;
  - se limita el escalado automático para que el grafo siga siendo legible al iniciar.
- Ajuste posterior del slide inicial:
  - se abrió la distribución horizontal del grafo;
  - se redujo el apilamiento vertical;
  - se ajustó el wrapping y el tamaño de etiquetas largas.
